cmake_minimum_required(VERSION 3.16)

# À compléter : nom réel du projet
project(ApplicationInformatique VERSION 0.1.0 LANGUAGES CXX)

set(CMAKE_CXX_STANDARD 17)
set(CMAKE_CXX_STANDARD_REQUIRED ON)

# À compléter : lister les sources réelles du projet
# file(GLOB_RECURSE SOURCES "src/*.cpp")

# À compléter : définir la cible (exécutable ou bibliothèque)
# add_executable(${PROJECT_NAME} ${SOURCES})

# À compléter : lier les dépendances externes éventuelles
# target_link_libraries(${PROJECT_NAME} PRIVATE ...)

# À compléter : activer les tests si le projet en contient (ex. CTest)
# enable_testing()
# add_subdirectory(tests)

# NOTE : ce fichier est un squelette. Si le projet n'utilise pas C/C++,
# ce fichier peut être supprimé.
