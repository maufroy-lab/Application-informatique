[project]
name = "application-informatique"
version = "0.1.0"
description = "À compléter : description du projet"
readme = "README.md"
requires-python = ">=3.10"
authors = [
    { name = "Romain Maufroy" }
]
dependencies = [
    # À compléter
]

[project.optional-dependencies]
dev = [
    "pytest",
    "ruff",
]

[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
